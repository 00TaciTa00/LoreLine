# 남은 작업 계획서

기준 시점: 2026-08-19, Cloudflare Workers 배포 완료 직후
(`https://loreline.seoyoung720.workers.dev` — 프로덕션에서 전 경로 200 확인)

원본 스펙의 기능 요구사항 8개 중 **6개는 완료**, 2개가 부분 구현 상태다.
아래는 스펙 대비 미충족분과 품질 부채를 우선순위로 정리한 것이다.

---

## 현재 상태 요약

| 스펙 요구사항 | 상태 |
| --- | --- |
| 1. World CRUD + 목록/선택/전환 | 완료 |
| 2. Event CRUD (공간·인물 필수 다중선택) | 완료 |
| 3. Place / Character CRUD + 목록 | 완료 |
| 4. 타임라인 시각화 (**수직/수평**) | **부분** — 수평만. 수직은 모바일 자동 폴백뿐, 사용자가 고를 수 없음 |
| 5. 뷰 전환 (전체/공간별/인물별 스윔레인) | 완료 |
| 6. 교차 탐색 | 완료 |
| 7. 태그/색상 구분 | 완료 |
| 8. 반응형 | 완료 |

추가로 API에는 있으나 UI가 없는 기능이 하나 있다: **사건 순서 재배치**
(`PATCH /events/:id`의 `afterEventId`).

---

## P1 — 스펙 미충족분

### 1-1. 수직/수평 방향 전환 토글

- **왜**: 스펙 4번이 "수직/수평"을 명시하는데 현재는 화면 폭에 따라 강제될 뿐
  사용자가 선택할 수 없다. 연표는 세로로 보는 편이 익숙한 사용자가 많다.
- **무엇**: 뷰 모드 토글 옆에 방향 토글(가로/세로)을 추가한다.
  - 세로 선택 시 기존 `VerticalEventList`를 재사용한다(이미 스윔레인 렌더링을 함).
  - 좁은 화면에서는 지금처럼 세로를 강제하되, 넓은 화면에서는 사용자 선택을 따른다.
- **범위**: `store/useTimelineViewStore.ts`에 `orientation` 상태 추가,
  `ViewToggle` 옆에 `OrientationToggle` 신규, `WorldTimelineView`의 분기 조건 수정.
- **주의**: `VerticalEventList`는 현재 모바일 전용 가정으로 패딩이 잡혀 있어
  데스크톱 폭에서 어색할 수 있다. 최대 폭 제한이 필요하다.

### 1-2. 사건 순서 재배치 UI

- **왜**: `sort_key` 삽입/재정렬 로직과 `afterEventId` API를 다 만들어 놓고
  UI가 없어 실제로 순서를 못 바꾼다. 작중 시간순 정렬이 이 도구의 핵심인데
  한번 만든 순서를 고칠 수단이 없는 상태다.
- **무엇**: 드래그앤드롭은 vis-timeline과 세로 리스트 양쪽에 각각 붙여야 해서
  비용이 크다. 대신 **사건 수정 모달에 "이 사건 다음으로 이동" 선택**을 넣는다.
  - 같은 세계관의 다른 사건 목록을 시간순으로 보여주고 하나를 고르면
    그 뒤로 이동. "맨 앞으로"도 선택지에 포함.
  - `afterEventId`를 그대로 쓰므로 서버 변경 없음.
- **범위**: `EventFormModal`에 select 추가, `useUpdateEvent` 호출에 `afterEventId` 전달.

---

## P2 — 품질/신뢰성

### 2-1. `sort_key` 로직 단위 테스트

- **왜**: `insertSortKey` / `appendSortKey`는 순수 함수인데 경계 조건
  (맨 앞 삽입, 맨 뒤 삽입, 간격 소진 → 재정렬 필요)이 까다롭고 조용히 틀리기 쉽다.
  정렬이 깨지면 사용자 데이터의 의미가 망가지는데 눈에 잘 안 띈다.
  현재 자동화 테스트가 **하나도 없다**.
- **무엇**: 테스트 러너(`vitest`) 도입 + `lib/db/sort-key.ts`의 순수 함수 테스트.
  - 빈 타임라인 / 맨 앞 / 맨 뒤 / 중간 / 간격 1 이하(재정렬 필요) 케이스
  - `computeLaneItems`의 다대다 중복 배치도 함께 테스트(스윔레인 정확성의 핵심)
- **왜 여기까지만**: DB를 타는 함수(`rebalanceTimeline` 등)는 테스트 DB 준비가 필요해
  비용 대비 효과가 떨어진다. 순수 함수만으로도 위험 구간 대부분을 덮는다.

### 2-2. 에러 상태 노출

- **왜**: 현재 mutation 실패 시 세계관 생성 폼을 제외하면 화면에 아무 표시가 없다.
  Neon이 scale-to-zero에서 깨어나며 지연/실패할 수 있는 구조라 무반응은 나쁘다.
- **무엇**: Place/Character/Event 모달의 저장·삭제 실패를 폼 안에 표시.
  (Event 모달은 이미 있음 → Place/Character 모달과 삭제 경로에 추가)

---

## P3 — 이번에 하지 않을 것

> **구식**: 이 절은 P1·P2 착수 전 시점의 판단이다. 현재 기준 남은 작업은
> 문서 맨 아래 "남은 작업 (2026-08-19 기준 최신화)"를 볼 것.

기록만 남기고 착수하지 않는다. 필요해지면 그때 판단한다.

- **여러 Timeline 관리 UI** — 스키마와 API는 다중 타임라인을 지원하지만
  UI는 세계관당 "메인 타임라인" 하나를 자동 사용한다. 스펙의 기능 요구사항에
  Timeline CRUD가 없어 후순위.
- **`sort_key` 부분 재정렬** — 현재 `rebalanceTimeline`은 타임라인 전체를 재채번한다.
  사건이 수천 건 쌓이기 전에는 문제가 되지 않는다.
- **E2E 테스트** — 브라우저 자동화로 전체 흐름 검증. 단위 테스트가 먼저다.
- **Neon pooled 연결 분리** — 현재 direct 문자열 하나를 앱과 마이그레이션이 공유한다.
  트래픽이 늘면 앱용 pooled(`-pooler`) 문자열을 따로 넣어야 한다.

---

## 진행 순서

1. P2-1 (테스트 인프라 + `sort_key` 테스트) — 이후 변경의 안전망이 되므로 먼저
2. P1-2 (순서 재배치 UI) — 테스트로 보호되는 정렬 로직 위에 UI를 얹음
3. P1-1 (방향 전환 토글)
4. P2-2 (에러 상태)

각 단계마다 타입체크·린트·빌드를 통과시키고 커밋한다.

---

## 진행 결과 (2026-08-19 완료)

P1·P2 전부 완료. 스펙 요구사항 8개가 모두 충족됐다.

- **P2-1**: vitest 도입, `sort-key` 11개 + `lanes` 11개 = 22개 테스트 통과.
- **P1-2**: 순서 재배치를 "작중 순서" select로 구현. 구현 중
  **API 결함을 발견해 함께 고쳤다** — `afterEventId: null`이 "맨 앞"이 아니라
  "맨 뒤"를 뜻해서 맨 앞으로 옮길 방법이 아예 없었다. 배치 대상을
  `InsertTarget`(`end`/`first`/`after`)으로 명시화하고 wire 포맷도
  `placement`("first" | "end" | 사건 id)로 교체했다.
- **P1-1**: 가로/세로 토글 추가. 좁은 화면에서는 세로가 강제되고 토글은
  비활성화 + 이유를 툴팁으로 안내한다.
- **P2-2**: Place/Character 모달의 저장·삭제 실패를 폼 안에 표시.

### 도중에 잡은 회귀

`useIsNarrowScreen`이 렌더 중 `window.matchMedia`를 읽어 **하이드레이션 불일치**를
일으키고 있었다(서버 `false` vs 클라이언트 `true`). React가 patch하지 않는 종류라
좁은 화면에서 방향 토글의 `disabled`/`title`이 잘못된 채로 남았다.
`useSyncExternalStore`(서버 스냅샷 분리)로 교체해 해결했다.

원인은 그 이전에 lint 규칙(`set-state-in-effect`)을 만족시키려고 effect의 setState를
렌더 중 초기화로 옮긴 것이었다. 린트는 통과했지만 하이드레이션을 깨뜨린 교환이었다.

---

# 남은 작업 (2026-08-19 기준 최신화)

앞선 P1·P2가 모두 끝나고 배포까지 마친 뒤 다시 정리한 목록이다.
위쪽 "P3 — 이번에 하지 않을 것"은 그 시점 기준이라, 이 절이 현재 기준으로 우선한다.

## 현재 상태

- 스펙 기능 요구사항 8개 **모두 충족**.
- 프로덕션: `https://loreline.seoyoung720.workers.dev` (Cloudflare **Workers**)
  — SSR 4개 페이지·API·`placement` 3모드·입력 검증 모두 동작 확인.
- `DATABASE_URL`은 Secret으로 등록됨(평문 var 아님).
- 단위 테스트 22개(`sort-key`, `lanes`), 타입체크·린트 통과.
- 원격 `main` = 로컬 `main` = `3a88518`.

> **2026-08-20 갱신**: A-2, B-3, C-2를 완료했다. A-1은 코드 쪽 준비를 마쳤고
> 대시보드 연결만 남았다. 각 항목에 결과를 적어뒀다.

## A. 배포 파이프라인 (가장 먼저 정리할 것)

### A-1. Workers Builds(Git 자동 배포) 연결 — **코드 준비 완료, 대시보드 작업 남음**

코드/설정 쪽에서 확인·정비한 것:

- `wrangler.jsonc`의 `name`이 배포된 Worker 이름과 일치(`loreline`).
  문서상 Workers Builds 실패의 대표 원인이라 먼저 확인했다.
- `.nvmrc`(24)로 CI npm 버전 고정 — push 완료.
- **`DATABASE_URL` 없이 `npm run cf:build`가 통과하는 것을 확인**했다.
  따라서 CI에 빌드 변수를 넣을 필요가 없다(DB는 요청 시점에만 접속).
- 저장소 루트에 `package.json`/`wrangler.jsonc`가 있어 Root directory는 비워도 된다.

남은 것은 **GitHub OAuth 승인이 필요해 사람이 직접 해야 한다.**
정확한 단계는 README의 "Git 자동 배포 (Workers Builds) 설정" 참고.

<details><summary>기존 설명</summary>
현재 배포는 로컬에서 `npm run cf:deploy`로 수동으로 한다. `main`에 push해도
자동 빌드가 돌지 않는 것을 확인했다(push 후 새 배포 없음).

연결하려면 대시보드 Workers & Pages > 프로젝트 > Settings > Builds에서:

| 항목 | 값 |
| --- | --- |
| Build command | `npm run cf:build` |
| Deploy command | `npx wrangler deploy` |

**주의**: Pages 프로젝트로 만들면 안 된다(이유는 README의 "배포 대상은 Pages가
아니라 Workers다" 참고). 또한 `.nvmrc`(Node 24) 없이는 CI가 Node 22 → npm 10으로
돌아 `npm ci`가 깨진다. `.nvmrc`는 push했지만 **아직 실제 CI로 검증되지 않았다** —
Workers Builds를 붙이는 첫 빌드가 그 검증이 된다.

</details>

### A-2. `master` 브랜치 정리 — **완료**
원격 `master`를 삭제했다. `main`이 그 내용을 모두 포함한다.

## B. 기능

### B-1. 사건 순서 드래그앤드롭 — **선택 사항으로 격하**
순서 변경 자체는 "작중 순서" select로 **이미 가능하다**. 따라서 이건 기능
공백이 아니라 UX 개선이다. 붙이려면 vis-timeline(가로)과 세로 리스트 양쪽에
각각 구현해야 하고, vis-timeline의 드래그 종료 이벤트를 `placement`로 변환하는
작업이 필요하다. 사건 수가 많아져 select가 불편해지면 그때 하는 것이 맞다.

### B-2. 복수 Timeline 관리 UI — 여전히 미구현
스키마·API는 세계관당 여러 타임라인을 지원하지만 UI는 "메인 타임라인" 하나를
자동 생성해 쓴다(`lib/db/timelines.ts`). 스펙 기능 요구사항에 Timeline CRUD가
없어 후순위로 둔 상태 그대로다.

### B-3. 세계관 삭제 시 하위 데이터 처리 — **완료**
실제 API로 확인해보니 예상보다 나빴다. 세계관을 지우면
`GET /api/worlds/:id`는 404인데 `/places`·`/characters`·`/events`는
**하위 데이터를 그대로 돌려주고 있었다**. 목록 필터가 각 테이블의
`deleted_at`만 보기 때문으로, 세계관 단위 격리 원칙이 깨진 상태였다.

`softDeleteWorld`(`lib/db/worlds.ts`)로 분리해 한 트랜잭션에서 하위
Timeline/Event/Place/Character까지 같은 `deleted_at`을 찍도록 고쳤다.
조인 테이블은 복구를 위해 보존한다(스펙의 CASCADE는 하드 삭제용).
테스트 7개로 덮었고 프로덕션에서도 확인했다.

남은 관련 이슈: **삭제된 세계관에 POST로 공간·인물을 새로 만들 수 있다.**
생성 API가 world의 `deleted_at`을 확인하지 않는다. 같은 계열의 구멍이지만
이번 범위(삭제 시 처리)를 넘어서 손대지 않았다.

## C. 품질

### C-1. E2E 테스트 — 미구현
단위 테스트는 순수 함수만 덮는다. 사건 생성→스윔레인 배치→교차 탐색으로
이어지는 흐름은 사람이 브라우저로 확인하고 있다.

### C-2. DB를 타는 로직의 테스트 — **완료**
PGlite(인메모리 Postgres)에 실제 마이그레이션을 적용하는 테스트 하네스
(`lib/db/test-db.ts`)를 만들고 `resolveSortKeyForInsert`/`rebalanceTimeline`을
12개 테스트로 덮었다. 간격이 소진되는 경계(간격 1, 정확히 `MIN_GAP`)와
재정렬을 반복 유발하는 케이스를 포함해, **미검증이던 재정렬 경로가 실제로
실행되는 것을 확인**했다. 네트워크가 필요 없어 CI에서도 그대로 돈다.

### C-3. `sort_key` 부분 재정렬 — 성능, 아직 불필요
`rebalanceTimeline`이 타임라인 전체를 재채번한다. 사건이 수천 건 쌓이기 전에는
문제가 되지 않는다.

### C-4. Neon pooled 연결 분리 — 트래픽 늘면
앱과 마이그레이션이 같은 direct 연결 문자열을 쓴다. 동시 접속이 늘면 앱용
pooled(`-pooler`) 문자열을 `DATABASE_URL`로, direct를 `DATABASE_URL_UNPOOLED`로
나눠야 한다(`.env.example`에 형식은 이미 적어둠).

## 권장 순서 (2026-08-20 갱신)

1. **A-1 대시보드 연결** — 코드 준비는 끝났다. 사람이 눌러야 하는 단계만 남았다.
2. **삭제된 세계관에 POST 차단** (B-3에서 파생) — 남은 격리 구멍.
3. 나머지(B-1 드래그앤드롭, B-2 복수 Timeline, C-1 E2E, C-3 부분 재정렬,
   C-4 pooled 연결)는 실제 사용하면서 필요해지는 순서대로.

완료: A-2(master 정리), B-3(삭제 시 하위 데이터), C-2(DB 로직 테스트).
