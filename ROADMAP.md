# 작업 관리

작업 항목은 **GitHub Issue**로 관리한다.

- 할 일: [Issues](https://github.com/00TaciTa00/LoreLine/issues)
- 왜 그렇게 했는지: [DECISIONS.md](./DECISIONS.md)

이 파일이 목록을 들고 있던 시절에는 갱신을 잊으면 그대로 낡았다. 실제로 커밋
아홉 개가 쌓이는 동안 문서가 그대로여서, 코드와 대조해 고쳐야 했던 적이 있다.
이슈는 커밋에서 `#12`로 가리키고 `closes #12`로 닫히므로 그런 어긋남이 줄어든다.

## 처음 한 번만: 이슈로 옮기기

아직 옮기지 않았다면 아래 두 단계를 실행한다.

```bash
gh auth login                          # 브라우저 승인 필요
bash scripts/setup-github-issues.sh
```

스크립트가 라벨·마일스톤·남은 작업 이슈를 만든다. 이미 있는 것은 건너뛰므로
다시 돌려도 안전하다. 저장소의 **Settings > General > Features > Issues**가
켜져 있어야 하며, 꺼져 있으면 스크립트가 그렇게 알려준다.

## 쓰는 방법

- **라벨**: `버그` `기능` `개선` `인프라` `문서` — 혼자 쓰는 저장소라 이 정도만 둔다
- **마일스톤**: `사용성 개선` / `테스트·품질` / `확장성`
- **커밋에서 잇기**: 본문에 `#12`를 적으면 이슈에 연결되고, `closes #12`면 머지 시 닫힌다
- **이슈 양식**: 버그 / 기능·개선 두 가지. 짧은 메모는 빈 이슈로 남겨도 된다

## 파일에 남기는 것

이슈로 옮기지 않는 것도 있다.

- [DECISIONS.md](./DECISIONS.md) — 배포 대상, DB 드라이버 선택처럼 **왜 그렇게
  했는지**. 이슈는 닫히면 시야에서 사라지지만 판단 근거는 계속 필요하다.
- [README.md](./README.md) — 실행법, 배포 설정, 알려진 함정

---

## 지난 기록

여기까지 온 과정과 도중에 내린 판단은 git 로그와 DECISIONS.md에 남아 있다.
스펙의 기능 요구사항 8개는 모두 충족했고, 이후 작업은 대부분 사용자 요청에서
나왔다.

주요 흐름:

1. 초기 세팅 → 스펙 기능 구현 → Cloudflare Workers 배포
2. 자동 배포(Workers Builds) 연결, `master` 브랜치 정리
3. 타임라인을 **세로축=시간 / 가로축=인물·공간 격자**로 재구성
   (vis-timeline은 시간축이 가로 고정이라 CSS Grid로 직접 그림)
4. 서식 편집기, 색상 피커, 검색식 다중 선택, 테마 전환 등 입력·표시 개선
5. 사건·공간·인물·시간 전부 끌어서 순서 변경
6. 작중 시각을 상위 기간 + 하위 시각으로 나누고, 상위 기간을 **시간 탭**으로 분리
